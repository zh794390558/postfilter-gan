apiVersion: v1
kind: Service
metadata:
  name: gan-service
  labels:
    name: gan
    role: service
spec:
    type: NodePort
    selector:
      name: gan 
    ports:
      - port: 80 
        targetPort: 6006 
        nodePort: 31111
